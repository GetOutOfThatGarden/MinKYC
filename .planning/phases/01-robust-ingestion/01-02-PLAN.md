---
phase: 01-robust-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "mobile/App/src/screens/MRZScanScreen.tsx",
  "mobile/App/src/utils/mrzParser.ts",
  "mobile/App/src/components/MRZCameraOverlay.tsx"
]
autonomous: true
requirements: [P1.2]
must_haves:
  truths:
    - "User can scan the MRZ zone of a passport using the camera."
    - "The app extracts Passport Number, Date of Birth, and Expiry Date from the MRZ."
    - "Scanning is fast and provides visual feedback when a valid MRZ is detected."
  artifacts:
    - path: "mobile/App/src/screens/MRZScanScreen.tsx"
      provides: "Camera interface for MRZ OCR"
    - path: "mobile/App/src/utils/mrzParser.ts"
      provides: "MRZ parsing logic"
  key_links:
    - from: "mobile/App/src/screens/MRZScanScreen.tsx"
      to: "mobile/App/src/utils/mrzParser.ts"
      via: "parser function call"
---

<objective>
Implement MRZ OCR scanning to automatically extract the keys required for NFC passport reading (BAC/PACE).

Purpose: Improve UX by eliminating manual entry of passport details.
Output: MRZ Scanning screen and parsing utility.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@mobile/App/src/screens/ScanScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>F1: Implement MRZ Parsing Utility</name>
  <files>mobile/App/src/utils/mrzParser.ts, mobile/App/src/utils/__tests__/mrzParser.test.ts</files>
  <behavior>
    - Parse TD3 (Passport) MRZ strings (2 lines, 44 chars each).
    - Extract: documentNumber, dateOfBirth (YYMMDD), expiryDate (YYMMDD).
    - Validate check digits for each field.
  </behavior>
  <action>
    Create a robust MRZ parser that handles TD3 format. 
    Use the check digit algorithm specified in ICAO Doc 9303.
    Export a function `parseMRZ(lines: string[])` returning the extracted keys.
  </action>
  <verify>
    <automated>cd mobile/App && npm test mrzParser.test.ts</automated>
  </verify>
  <done>MRZ parser implemented and verified with unit tests for various passport examples.</done>
</task>

<task type="auto">
  <name>F2: Create MRZ Scanning Screen</name>
  <files>mobile/App/src/screens/MRZScanScreen.tsx, mobile/App/src/components/MRZCameraOverlay.tsx</files>
  <action>
    Implement a new screen `MRZScanScreen` using `react-native-vision-camera`.
    Use a Frame Processor (or the library's built-in OCR if using a specific MRZ plugin) to detect MRZ text in real-time.
    Add a `MRZCameraOverlay` to guide the user to align the MRZ zone.
    Once a valid MRZ is parsed and check-digits pass, navigate to the NFC reading screen with the extracted data.
  </action>
  <verify>
    <automated>cd mobile/App && npx tsc --noEmit</automated>
  </verify>
  <done>MRZ scanning UI implemented and integrated with the parser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Camera → Memory | Raw image frames processed locally; no data leaves the device. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | MRZ Buffer | mitigate | Clear MRZ data from memory as soon as the NFC session starts. |
</threat_model>

<verification>
Manual verification: Open the MRZ Scan screen and point it at a passport MRZ. Ensure it correctly identifies the zone and extracts the data.
</verification>

<success_criteria>
- MRZ parser passes all test cases including check digit validation.
- Camera screen successfully captures and parses MRZ from a physical or mock passport.
</success_criteria>

<output>
After completion, create `.planning/phases/01-robust-ingestion/01-02-SUMMARY.md`
</output>
