---
phase: 01-robust-ingestion
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "mobile/App/src/hooks/useNFC.ts",
  "mobile/App/package.json",
  "mobile/App/src/components/NFCReadingProgress.tsx"
]
autonomous: true
requirements: [P1.3]
must_haves:
  truths:
    - "App supports both BAC and PACE protocols for passport reading."
    - "NFC reading provides real-time progress feedback to the user."
    - "The app uses haptic feedback to signal successful tag detection and completion."
  artifacts:
    - path: "mobile/App/src/hooks/useNFC.ts"
      provides: "Unified NFC reading hook"
    - path: "mobile/App/src/components/NFCReadingProgress.tsx"
      provides: "Visual feedback during NFC read"
  key_links:
    - from: "mobile/App/src/hooks/useNFC.ts"
      to: "@2060.io/react-native-eid-reader"
      via: "import and method calls"
---

<objective>
Integrate the high-reliability `@2060.io/react-native-eid-reader` library to support modern passports (PACE) and improve the overall success rate of NFC scanning.

Purpose: Universal support for modern ePassports and better error handling.
Output: Upgraded NFC hook and reading progress UI.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/NFC_PASSPORT_RESEARCH.md
@mobile/App/src/hooks/useNFC.ts
</context>

<tasks>

<task type="auto">
  <name>A3: Integrate EID Reader Library</name>
  <files>mobile/App/package.json, mobile/App/src/hooks/useNFC.ts</files>
  <action>
    Install `@2060.io/react-native-eid-reader`.
    Remove `react-native-nfc-passport-reader` (legacy library).
    Update `useNFC.ts` to use the new library. 
    Ensure PACE support is enabled and the library's error handling is properly mapped to the hook's state.
  </action>
  <verify>
    <automated>cd mobile/App && npx tsc --noEmit</automated>
  </verify>
  <done>New library integrated and legacy library removed.</done>
</task>

<task type="auto">
  <name>F3: Implement Progress UI & Haptics</name>
  <files>mobile/App/src/components/NFCReadingProgress.tsx, mobile/App/src/hooks/useNFC.ts</files>
  <action>
    Create a `NFCReadingProgress` component that displays the current step (BAC, PACE, Reading DG1, Reading DG2) and a progress percentage.
    Update `useNFC` to emit progress events.
    Integrate `react-native-haptic-feedback` to trigger:
    - 'impactLight' on tag detection.
    - 'notificationSuccess' on completion.
    - 'notificationError' on failure.
  </action>
  <verify>
    <automated>cd mobile/App && npx tsc --noEmit</automated>
  </verify>
  <done>Visual and tactile feedback integrated into the scanning flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Passport Chip → App | Secure Messaging (SM) ensures data is encrypted during transit between chip and app. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | NFC APDUs | mitigate | Rely on Secure Messaging (PACE/BAC) and Passive Authentication (in later phases). |
</threat_model>

<verification>
Manual verification: Attempt to read a passport (physical or known mock tag). Verify that progress updates are displayed and haptics trigger correctly.
</verification>

<success_criteria>
- `useNFC` successfully initializes and interacts with the `@2060.io/react-native-eid-reader`.
- Reading flow completes with PACE-enabled passports (if available for testing).
- Progress UI correctly reflects the state of the long-running NFC read.
</success_criteria>

<output>
After completion, create `.planning/phases/01-robust-ingestion/01-03-SUMMARY.md`
</output>
