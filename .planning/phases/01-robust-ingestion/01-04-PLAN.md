---
phase: 01-robust-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: [
  "mobile/App/src/constants/mockProfiles.ts",
  "mobile/App/src/screens/__tests__/IngestionFlow.test.tsx"
]
autonomous: true
requirements: [P1.4]
must_haves:
  truths:
    - "Automated tests verify that the MRZ data correctly triggers the NFC reading session."
    - "Mock passport profiles are available for testing different scenarios (Over 18, Nationality)."
    - "The end-to-end ingestion flow (Scan -> Parse -> Read) is validated."
  artifacts:
    - path: "mobile/App/src/constants/mockProfiles.ts"
      provides: "Standardized test data"
    - path: "mobile/App/src/screens/__tests__/IngestionFlow.test.tsx"
      provides: "E2E component tests"
  key_links:
    - from: "mobile/App/src/screens/__tests__/IngestionFlow.test.tsx"
      to: "mobile/App/src/screens/MRZScanScreen.tsx"
      via: "render and fireEvent"
---

<objective>
Establish a robust testing framework for the ingestion flow using mock profiles to ensure reliability and prevent regressions.

Purpose: Guarantee that the foundation of the identity stack is solid before adding ZK logic.
Output: Mock profiles and automated E2E tests.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@mobile/App/src/constants/mockProfiles.ts
</context>

<tasks>

<task type="auto">
  <name>T1: Expand Mock Passport Profiles</name>
  <files>mobile/App/src/constants/mockProfiles.ts</files>
  <action>
    Create a set of mock passport profiles including:
    - Adult (Over 18)
    - Minor (Under 18)
    - Different nationalities (US, UK, DE, FR)
    - Expired passport scenario.
    Each profile should include the raw data, the corresponding MRZ lines, and the expected extracted keys.
  </action>
  <verify>
    <automated>grep -q "documentNumber" mobile/App/src/constants/mockProfiles.ts</automated>
  </verify>
  <done>Comprehensive mock profiles available for testing.</done>
</task>

<task type="auto">
  <name>V1: Implement E2E Ingestion Flow Test</name>
  <files>mobile/App/src/screens/__tests__/IngestionFlow.test.tsx</files>
  <action>
    Write a React Native Testing Library test that:
    1. Mocks the Camera output to provide a valid MRZ image/string.
    2. Verifies the `MRZScanScreen` correctly parses the input.
    3. Verifies navigation to the reading state.
    4. Mocks the NFC reader to simulate a successful passport read from the extracted keys.
    5. Asserts that the final Identity object is correctly populated.
  </action>
  <verify>
    <automated>cd mobile/App && npm test IngestionFlow.test.tsx</automated>
  </verify>
  <done>End-to-end ingestion flow verified through automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Data → Code | Mock profiles must reflect realistic passport structures to avoid false positives. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Repudiation | Test Logs | accept | Test logs are for development use only. |
</threat_model>

<verification>
Run `npm test` and ensure all tests in `IngestionFlow.test.tsx` pass.
</verification>

<success_criteria>
- Mock profiles cover all critical business logic scenarios.
- Automated tests consistently pass, covering the full path from OCR to successful NFC read.
</success_criteria>

<output>
After completion, create `.planning/phases/01-robust-ingestion/01-04-SUMMARY.md`
</output>
