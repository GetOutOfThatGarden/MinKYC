---
phase: 02-zk-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [circuits/src/main.nr, circuits/Nargo.toml]
autonomous: true
requirements: [P2.1, P2.3]
must_haves:
  truths:
    - "Circuit compiles with Noir v0.36.0+"
    - "Nullifier is deterministically generated from passport UID"
    - "Proof fails if passport data does not match commitment"
  artifacts:
    - path: "circuits/src/main.nr"
      provides: "ZK circuit logic"
    - path: "circuits/Nargo.toml"
      provides: "Circuit configuration"
---

<objective>
Port Noir circuits to the latest version (v0.36.0+) and implement "Nullifier" logic for Sybil resistance and unlinkability.

Purpose: Ensure the ZK engine is compatible with modern Barretenberg backends and provides privacy-preserving Sybil resistance.
Output: Validated Noir circuits with Nullifier support.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@circuits/src/main.nr
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port circuit to Noir v0.36.0+</name>
  <files>circuits/src/main.nr, circuits/Nargo.toml</files>
  <behavior>
    - Circuit compiles with `nargo compile`
    - Tests pass with `nargo test`
    - Modern syntax used (e.g., `pub` keyword placement, dependency versions in Nargo.toml)
  </behavior>
  <action>
    - Update `circuits/Nargo.toml` to target latest Noir version and dependencies.
    - Refactor `circuits/src/main.nr` to use modern Noir syntax.
    - Change `main` return type to `pub` or use assertions as per latest best practices.
    - Use `std::hash::sha256` for commitment binding instead of simple addition.
  </action>
  <verify>
    <automated>cd circuits && nargo test</automated>
  </verify>
  <done>Circuit compiles and all existing tests pass under the new version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Nullifier logic</name>
  <files>circuits/src/main.nr</files>
  <behavior>
    - `compute_nullifier(passport_uid, secret)` returns a deterministic Field.
    - Nullifier is included as a public output of the circuit.
    - Different secrets for the same passport_uid produce different nullifiers.
  </behavior>
  <action>
    - Add a private input `passport_uid` (unique identifier from NFC chip).
    - Add a function to compute a nullifier using `std::hash::poseidon` or `std::hash::sha256` (Poseidon is better for ZK efficiency if supported).
    - Update `main` to include `nullifier` as a public output.
    - Add TDD tests for nullifier uniqueness and determinism.
  </action>
  <verify>
    <automated>cd circuits && nargo test --test test_nullifier</automated>
  </verify>
  <done>Nullifier logic is integrated and verified with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Circuit | Private inputs (passport data, secrets) must remain private. |
| Circuit -> Verifier | Public outputs (nullifier, commitment) must be verifiable. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | circuits/src/main.nr | mitigate | Ensure sensitive fields like `passport_uid` and `secret` are private inputs. |
| T-02-02 | Tampering | circuits/src/main.nr | mitigate | Use SHA256/Poseidon for commitment binding to prevent input manipulation. |
</threat_model>

<verification>
- `nargo check` passes without warnings.
- `nargo test` passes all tests including new nullifier tests.
</verification>

<success_criteria>
- Circuit logic is compatible with Noir v0.36.0+.
- Nullifiers are correctly generated and exposed.
- Privacy of raw passport data is maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/02-zk-engine/02-01-SUMMARY.md`
</output>
