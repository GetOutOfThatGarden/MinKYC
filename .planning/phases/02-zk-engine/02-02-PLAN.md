---
phase: 02-zk-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [mobile/App/metro.config.js, mobile/App/src/components/ZKProver.tsx, mobile/App/app.json]
autonomous: true
requirements: [P2.2, Z2, Z3]
must_haves:
  truths:
    - "WASM files are successfully bundled as assets in the Expo app"
    - "WebView can load and execute Noir/Barretenberg WASM"
    - "Real ZK proofs are generated from verified passport data"
  artifacts:
    - path: "mobile/App/metro.config.js"
      provides: "WASM asset support"
    - path: "mobile/App/src/components/ZKProver.tsx"
      provides: "Noir ZK Prover implementation"
---

<objective>
Implement high-performance ZKProver using Noir/Barretenberg WASM inside the React Native WebView and solve the WASM asset bundling bottleneck.

Purpose: Transition from mock SHA-256 to real ZK proofs running on-device.
Output: Mobile app capable of generating real Noir ZK proofs.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@mobile/App/src/components/ZKProver.tsx
@mobile/App/metro.config.js
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure WASM bundling in Metro</name>
  <files>mobile/App/metro.config.js, mobile/App/app.json</files>
  <action>
    - Update `metro.config.js` to include `wasm` in `resolver.assetExts`.
    - Ensure `app.json` includes `wasm` files in the `packagerOpts` or `assets` list if required by the Expo version.
    - This allows the app to include the WASM binaries from `@noir-lang/backend_barretenberg` as local assets.
  </action>
  <verify>
    <automated>grep "wasm" mobile/App/metro.config.js</automated>
  </verify>
  <done>Metro is configured to bundle .wasm files.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real ZK Prover in WebView</name>
  <files>mobile/App/src/components/ZKProver.tsx</files>
  <action>
    - Update the HTML/JavaScript inside `ZKProver.tsx` to import `@noir-lang/noir_js` and `@noir-lang/backend_barretenberg`.
    - Since standard ESM imports might fail in WebView with local assets, use a bundled approach or a script that loads the WASM using `fetch` on the local asset URI.
    - Replace the mock SHA-256 logic with:
      1. `const backend = new UltraHonkBackend(circuitBytecode);`
      2. `const noir = new Noir(circuit, backend);`
      3. `const { witness } = await noir.execute(inputs);`
      4. `const proof = await backend.generateProof(witness);`
    - Update input mapping to include the new `passport_uid` and `nullifier` fields from Plan 01.
  </action>
  <verify>
    <automated>npm test mobile/App/src/components/ZKProver.tsx -- -u</automated>
  </verify>
  <done>ZKProver generates real Noir proofs in the WebView environment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real Noir ZK Prover integrated into the Mobile App</what-built>
  <how-to-verify>
    1. Run the app: `cd mobile/App && npm run android` (or ios).
    2. Navigate to the Identity Scan flow.
    3. Complete a scan and trigger "Generate Proof".
    4. Verify that the "Step 3" log now shows real Noir/Barretenberg execution instead of SHA-256 mock.
    5. Ensure a proof is successfully generated without crashing the app.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WebView -> Main Thread | Proof and public inputs are passed back via `postMessage`. |
| Local Storage -> WebView | WASM assets must be loaded securely. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Mobile App | mitigate | Monitor memory usage during WASM execution; use UltraHonk for smaller proofs. |
| T-02-04 | Information Disclosure | mobile/App/src/components/ZKProver.tsx | mitigate | Ensure WebView does not leak private inputs to external domains (use `originWhitelist`). |
</threat_model>

<verification>
- Proof generation completes successfully on a physical device or emulator.
- Proof size and public inputs match expectations for the Noir circuit.
</verification>

<success_criteria>
- Transition from mock to real ZK proofs is complete.
- WASM bundling bottleneck is resolved.
- App remains stable during proof generation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-zk-engine/02-02-SUMMARY.md`
</output>
