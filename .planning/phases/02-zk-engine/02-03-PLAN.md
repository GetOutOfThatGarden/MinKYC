---
phase: 02-zk-engine
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [mobile/App/src/utils/benchmarks.ts, mobile/App/src/screens/IdentityScreen.tsx]
autonomous: true
requirements: [P2.4]
must_haves:
  truths:
    - "Proof generation latency is accurately measured and logged"
    - "Memory usage during proof generation is monitored"
    - "Benchmark data is presented in a readable format"
  artifacts:
    - path: "mobile/App/src/utils/benchmarks.ts"
      provides: "Benchmarking utility"
---

<objective>
Benchmark proof generation latency and memory usage on mobile across different device profiles.

Purpose: Quantify the performance of the Universal ZK-Engine and identify areas for optimization.
Output: Performance report and benchmarking utility.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Benchmarking Utility</name>
  <files>mobile/App/src/utils/benchmarks.ts</files>
  <action>
    - Create a utility to measure:
      1. Time to load WASM binaries.
      2. Time to compute witness.
      3. Time to generate proof.
      4. Total end-to-end ZK latency.
    - Implement simple memory monitoring using `performance.memory` (if available in WebView) or by tracking native heap before/after.
  </action>
  <verify>
    <automated>ls mobile/App/src/utils/benchmarks.ts</automated>
  </verify>
  <done>Benchmarking utility is created.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Benchmarks into UI</name>
  <files>mobile/App/src/screens/IdentityScreen.tsx</files>
  <action>
    - Update `IdentityScreen` to display the benchmarking results after a successful proof generation.
    - Log results to a local file or console for analysis.
    - Add a "Debug Mode" toggle to show/hide performance metrics.
  </action>
  <verify>
    <automated>grep "benchmark" mobile/App/src/screens/IdentityScreen.tsx</automated>
  </verify>
  <done>Benchmarks are visible in the app for performance testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Performance Benchmarking Suite</what-built>
  <how-to-verify>
    1. Run the app on a physical device.
    2. Complete the ZK proof generation flow.
    3. Observe the latency metrics displayed in the app or logs.
    4. Verify that the total time matches expectations (e.g., < 60s for UltraHonk on modern devices).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> Performance API | Benchmarking data is collected locally. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | mobile/App/src/utils/benchmarks.ts | accept | Benchmarking data does not contain PII; risk is low. |
</threat_model>

<verification>
- Latency and memory data are successfully captured and displayed.
- The app remains responsive during benchmarking.
</verification>

<success_criteria>
- Performance of the ZK engine is quantified.
- Benchmarks guide future optimization efforts (e.g., circuit simplification or backend switching).
</success_criteria>

<output>
After completion, create `.planning/phases/02-zk-engine/02-03-SUMMARY.md`
</output>
