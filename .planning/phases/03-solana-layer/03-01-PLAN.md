---
phase: 03-solana-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [programs/minkyc/src/lib.rs]
autonomous: true
requirements: [P3.1]
must_haves:
  truths:
    - "Anchor program handles NullifierReceipt PDAs for replay protection"
    - "Verification fails if caller_pubkey public input doesn't match signer"
    - "Identity stores Field-based commitments (32 bytes)"
  artifacts:
    - path: "programs/minkyc/src/lib.rs"
      provides: "Updated Anchor program with privacy features"
---

<objective>
Upgrade the Anchor program to support Nullifier-based verification and Proof Binding. This ensures that verifications are unlinkable (privacy) and cannot be stolen or replayed (security).

Purpose: Transition from identity-linked receipts to privacy-preserving nullifiers.
Output: Refactored Anchor program with Nullifier and Binding logic.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@programs/minkyc/src/lib.rs
@circuits/src/main.nr
@.planning/research/SOLANA_REPLAY_PROTECTION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Identity for Field commitments</name>
  <files>programs/minkyc/src/lib.rs</files>
  <behavior>
    - `Identity` struct uses `[u8; 32]` for `commitment`.
    - `initialize` function accepts `[u8; 32]` commitment.
    - Code is documented to specify that this represents a BN254 Field element.
  </behavior>
  <action>
    - Verify `Identity` struct in `programs/minkyc/src/lib.rs` uses `[u8; 32]` for the `commitment` field.
    - Add comments to the `Identity` struct and `initialize` instruction clarifying that the commitment is a Noir/BN254 Field element.
    - Ensure the `Initialize` context and instruction correctly handle the 32-byte input.
  </action>
  <verify>
    <automated>anchor build</automated>
  </verify>
  <done>Identity commitment is correctly defined as a 32-byte Field representation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NullifierReceipt PDA</name>
  <files>programs/minkyc/src/lib.rs</files>
  <behavior>
    - A new account type `NullifierReceipt` exists.
    - `VerifyProof` context uses `nullifier` as a seed for `NullifierReceipt` PDA.
    - Initializing the same `nullifier` twice fails with `AccountAlreadyInitialized`.
  </behavior>
  <action>
    - Define `NullifierReceipt` account struct with fields: `used: bool`, `nullifier: [u8; 32]`, `timestamp: i64`, `identity: Pubkey`.
    - Create/Update `VerifyProof` context to include `nullifier: [u8; 32]` in instructions.
    - Use `seeds = [b"nullifier", nullifier.as_ref()]` for the `nullifier_receipt` account in `VerifyProof`.
    - Remove the old `ProofReceipt` logic that was seeded by `proof_hash` and `identity` to ensure unlinkability.
  </action>
  <verify>
    <automated>anchor build</automated>
  </verify>
  <done>NullifierReceipt PDA is implemented for unlinkable replay protection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Proof Binding & Verification Logic</name>
  <files>programs/minkyc/src/lib.rs</files>
  <behavior>
    - `verify_proof` instruction accepts `proof`, `nullifier`, `caller_pubkey`, and `requirement_hash`.
    - Logic checks if `caller_pubkey` (passed as public input) matches `ctx.accounts.verifier.key()`.
    - Returns `BindingMismatch` error if they do not match.
    - Marks `NullifierReceipt` as used.
  </behavior>
  <action>
    - Add `BindingMismatch` to `ErrorCode`.
    - Update `verify_proof` instruction signature to include `nullifier` and `caller_pubkey`.
    - Inside `verify_proof`, add an assertion: `require_keys_eq!(caller_pubkey, ctx.accounts.verifier.key(), ErrorCode::BindingMismatch)`.
    - Populate the `NullifierReceipt` account with metadata.
    - Keep the mock ZK verification check for now (proof must be non-zero), as the real Groth16/Plonk verifier integration is typically a separate sub-phase or specialized task.
  </action>
  <verify>
    <automated>anchor build</automated>
  </verify>
  <done>Proof Binding and Nullifier logic are integrated into the verify_proof instruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Verifier -> Program | Verifier submits proof and public inputs. |
| Program -> Nullifier PDA | Program creates a persistent record of the nullifier. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | verify_proof | mitigate | Bind proof to `caller_pubkey` to prevent front-running/theft. |
| T-03-02 | Repudiation | nullifier_receipt | mitigate | Use Nullifier-based PDAs to prevent multiple verifications for the same secret/context. |
| T-03-03 | Information Disclosure | nullifier_receipt | mitigate | Remove `identity_pda` from Nullifier seeds to ensure verifications are unlinkable to the identity. |
</threat_model>

<verification>
- `anchor build` succeeds.
- Program logic correctly uses `nullifier` for PDA seeds.
- Binding check is present in `verify_proof`.
</verification>

<success_criteria>
- Solana program supports Nullifier-based replay protection.
- Proofs are cryptographically bound to the transaction signer.
- Identity records remain private during verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-solana-layer/03-01-SUMMARY.md`
</output>
