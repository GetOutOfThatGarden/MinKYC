---
phase: 03-solana-layer
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [mobile/App/src/screens/VerifyScreen.tsx, mobile/App/src/utils/solana.ts, mobile/App/src/components/ZKProver.tsx]
autonomous: true
requirements: [P3.1]
must_haves:
  truths:
    - "Mobile app includes wallet public key as a binding input to ZK proof"
    - "Mobile app derives NullifierReceipt PDA for on-chain replay protection"
    - "Mobile app submits real transaction to Solana devnet"
  artifacts:
    - path: "mobile/App/src/utils/solana.ts"
      provides: "Updated Solana utilities with Nullifier PDA derivation"
    - path: "mobile/App/src/screens/VerifyScreen.tsx"
      provides: "On-chain verification submission logic"
---

<objective>
Integrate the upgraded Solana privacy logic into the mobile application. This involves updating the ZK input generation to include proof binding (caller pubkey) and implementing the on-chain submission flow using Nullifier PDAs.

Purpose: Bridge the mobile ZK engine with the Solana Privacy Layer.
Output: Mobile app capable of submitting bound, privacy-preserving proofs to Solana.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@mobile/App/src/screens/VerifyScreen.tsx
@mobile/App/src/utils/solana.ts
@mobile/App/src/components/ZKProver.tsx
@programs/minkyc/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ZK Prover with Binding Input</name>
  <files>mobile/App/src/components/ZKProver.tsx, mobile/App/src/screens/VerifyScreen.tsx</files>
  <action>
    - Update `ZKProverProps` and `inputs` interface in `ZKProver.tsx` to ensure `caller_pubkey` is expected as a number/string (Field representation).
    - In `VerifyScreen.tsx`, update the `generateProof` logic to include the current `publicKey` (from `useWallet`) in the `zkInputs`.
    - Convert the `publicKey` (32 bytes) to a Field-compatible representation (e.g., slicing or BigInt conversion) before passing to `ZKProver`.
  </action>
  <verify>
    <automated>cd mobile/App && npm run lint</automated>
  </verify>
  <done>ZK Proof generation now includes the wallet pubkey as a public input for binding.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Nullifier PDA Derivation</name>
  <files>mobile/App/src/utils/solana.ts</files>
  <action>
    - Add `getNullifierReceiptPda` function to `mobile/App/src/utils/solana.ts`.
    - Function should take a `nullifier` (Uint8Array or Buffer) and return the PDA and bump.
    - Seeds must match the Anchor program: `[Buffer.from('nullifier'), nullifier]`.
  </action>
  <verify>
    - Manual: Check `solana.ts` for the new function and matching seeds.
  </verify>
  <done>Client-side PDA derivation for nullifiers is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement On-Chain Verification Submission</name>
  <files>mobile/App/src/screens/VerifyScreen.tsx</files>
  <action>
    - In `VerifyScreen.tsx`, update `handleProofGenerated` to perform a real Solana transaction.
    - Extract the `nullifier` from the `publicInputs` array (index based on `main.nr` return/output order).
    - Use `getNullifierReceiptPda` to find the receipt account address.
    - Construct the `verify_proof` instruction using `@solana/web3.js` (matching the Anchor instruction discriminator).
    - Submit the transaction to Devnet and update the UI with the real `txSignature`.
  </action>
  <verify>
    - Manual: Generate a proof in the app and verify the transaction is sent to devnet (logs will show the signature).
  </verify>
  <done>Mobile app successfully submits privacy-preserving proofs to the Solana blockchain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Wallet -> Mobile App | App signs transaction using the user's private key. |
| Mobile App -> Solana RPC | Proof and public inputs are sent to the network. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | ZK Inputs | mitigate | Ensure `secret_nonce` and `dob` are never logged or sent to RPC as public inputs. |
| T-03-05 | Tampering | Transaction Builder | mitigate | Bind the proof to the transaction's `feePayer` or `signer` (Task 1) to prevent interception. |
</threat_model>

<verification>
- `VerifyScreen.tsx` compiles without errors.
- Proof generation succeeds with the new binding input.
- Transaction submission logs show the correct Nullifier PDA being used.
</verification>

<success_criteria>
- Mobile app generates proofs bound to the user's wallet.
- Mobile app derives correct PDAs for nullifier-based replay protection.
- End-to-end verification flow works from device to devnet.
</success_criteria>

<output>
After completion, create `.planning/phases/03-solana-layer/03-02-SUMMARY.md`
</output>
