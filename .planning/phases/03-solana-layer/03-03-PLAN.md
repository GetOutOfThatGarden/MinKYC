---
phase: 03-solana-layer
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [tests/minkyc.ts]
autonomous: true
requirements: [P3.3]
must_haves:
  truths:
    - "Test suite verifies that a nullifier cannot be used twice"
    - "Test suite verifies that proofs are rejected if signed by wrong wallet"
    - "Test suite verifies Field-based commitment initialization"
  artifacts:
    - path: "tests/minkyc.ts"
      provides: "Updated test suite for privacy layer verification"
---

<objective>
Implement comprehensive automated tests for the Solana Privacy Layer. These tests verify the cryptographic binding and nullifier-based replay protection at the Anchor program level.

Purpose: Ensure the security and privacy guarantees of the Solana program are robust and regression-tested.
Output: Validated test suite for Phase 3 features.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@tests/minkyc.ts
@programs/minkyc/src/lib.rs
@.planning/research/SOLANA_REPLAY_PROTECTION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Nullifier Uniqueness & Replay Protection</name>
  <files>tests/minkyc.ts</files>
  <behavior>
    - Test 1: Successful verification creates a `NullifierReceipt` account.
    - Test 2: Attempting to verify with the SAME nullifier again fails with `AlreadyInitialized` or custom `ProofAlreadyUsed` error.
    - Test 3: Verifying with a DIFFERENT nullifier (new proof/secret) succeeds.
  </behavior>
  <action>
    - Update `tests/minkyc.ts` to generate mock 32-byte nullifiers for testing.
    - Derive the `NullifierReceipt` PDA in the test suite using `PublicKey.findProgramAddressSync`.
    - Implement a test case that calls `verifyProof` twice with the same nullifier and asserts that the second call fails.
    - Implement a test case that verifies different nullifiers work correctly.
  </action>
  <verify>
    <automated>anchor test</automated>
  </verify>
  <done>Nullifier-based replay protection is fully verified by the test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Proof Binding (Signer Verification)</name>
  <files>tests/minkyc.ts</files>
  <behavior>
    - Test 1: Verification succeeds if `verifier.publicKey` matches the `caller_pubkey` passed in instruction.
    - Test 2: Verification fails if a DIFFERENT wallet signs the transaction than the one specified in `caller_pubkey` public input.
  </behavior>
  <action>
    - Create a test case where `caller_pubkey` (public input) matches the transaction signer.
    - Create a test case where `caller_pubkey` is set to one wallet address, but a different `Keypair` signs the transaction.
    - Assert that the second case fails with the expected `BindingMismatch` error.
  </action>
  <verify>
    <automated>anchor test</automated>
  </verify>
  <done>Proof binding security is fully verified by the test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Runner -> Anchor | Test suite acts as an adversarial verifier. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Nullifier PDAs | accept | PDA initialization costs rent, which naturally prevents spam; tests ensure functionality. |
</threat_model>

<verification>
- `anchor test` passes all tests.
- Replay attempts are correctly blocked.
- Binding mismatches are correctly blocked.
</verification>

<success_criteria>
- 100% pass rate for privacy layer tests.
- Replay protection is cryptographically sound (via PDAs).
- Proof binding prevents front-running in simulated scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-solana-layer/03-03-SUMMARY.md`
</output>
