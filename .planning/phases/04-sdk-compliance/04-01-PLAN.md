---
phase: 04-sdk-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sdk/package.json, sdk/tsconfig.json, sdk/src/index.ts, sdk/src/verifier.ts, sdk/src/types.ts]
autonomous: true
requirements: [P4.1]
must_haves:
  truths:
    - "SDK package is initialized and builds successfully"
    - "ProofRequest interface is defined according to Mobile App expectations"
    - "MinKYCClient can connect to Solana and interact with the Anchor program"
  artifacts:
    - path: "sdk/package.json"
      provides: "Package metadata and dependencies"
    - path: "sdk/src/index.ts"
      provides: "Main entry point and client export"
    - path: "sdk/src/verifier.ts"
      provides: "On-chain verification wrapper"
  key_links:
    - from: "sdk/src/verifier.ts"
      to: "programs/minkyc/src/lib.rs"
      via: "@coral-xyz/anchor"
      pattern: "program.methods.verifyProof"
---

<objective>
Initialize the @minkyc/sdk TypeScript library and implement the core on-chain verification wrapper.

Purpose: Provide a unified interface for dApps to request and verify MinKYC proofs.
Output: A buildable TypeScript SDK in the `sdk/` directory.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@programs/minkyc/src/lib.rs
@cli/src/utils/connection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup SDK Project Structure</name>
  <files>sdk/package.json, sdk/tsconfig.json, sdk/src/index.ts</files>
  <action>
    Create the `sdk/` directory. 
    Initialize `package.json` with name `@minkyc/sdk`, version `0.1.0`.
    Add dependencies: `@solana/web3.js`, `@coral-xyz/anchor`, `bn.js`.
    Configure `tsconfig.json` for ESM output.
    Create a placeholder `src/index.ts` with basic exports.
    Per D1.
  </action>
  <verify>
    <automated>cd sdk && npm install && npx tsc</automated>
  </verify>
  <done>SDK directory exists and TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Core SDK Logic</name>
  <files>sdk/src/types.ts, sdk/src/verifier.ts, sdk/src/index.ts</files>
  <action>
    Define `ProofRequest` interface in `types.ts` (should include requirements like 'over-18', 'nationality', etc.).
    Implement `MinKYCClient` in `verifier.ts`.
    Add `verifyProofOnChain` method:
    - Should accept the proof (Noir output), nullifier, and proof receipt PDA.
    - Interact with the Anchor program (per Phase 3 implementation) using the provided wallet/connection.
    Export `MinKYCClient` and types from `index.ts`.
    Per D1.
  </action>
  <verify>
    <automated>npx tsc -p sdk/tsconfig.json</automated>
  </verify>
  <done>MinKYCClient is implemented with on-chain verification logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Solana RPC | Untrusted RPC node could return stale/fake data |
| SDK → dApp | SDK must ensure proof binding matches the dApp's expected user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | sdk/src/verifier.ts | mitigate | Validate Proof Binding matches the caller's pubkey before calling on-chain verify |
| T-04-01-02 | Information Disclosure | sdk/src/types.ts | mitigate | Ensure ProofRequest only contains public metadata, no PII placeholders |
</threat_model>

<verification>
- `npm run build` in `sdk/` directory succeeds.
- Basic unit test (mocking Anchor) confirms `MinKYCClient` correctly formats the instruction.
</verification>

<success_criteria>
The @minkyc/sdk is ready for integration, with working on-chain verification logic and exported types.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sdk-compliance/04-01-SUMMARY.md`
</output>
