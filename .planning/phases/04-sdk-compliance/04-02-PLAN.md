---
phase: 04-sdk-compliance
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [sdk/src/hooks/useMinKYC.ts, sdk/src/index.ts, website/src/App.tsx, website/src/components/DeveloperSection.tsx]
autonomous: true
requirements: [P4.2]
must_haves:
  truths:
    - "React developers can use useMinKYC to initiate verification"
    - "The MinKYC website provides clear documentation for the SDK"
  artifacts:
    - path: "sdk/src/hooks/useMinKYC.ts"
      provides: "React integration hook"
    - path: "website/src/components/DeveloperSection.tsx"
      provides: "Interactive developer dashboard"
  key_links:
    - from: "sdk/src/hooks/useMinKYC.ts"
      to: "sdk/src/verifier.ts"
      via: "import"
      pattern: "import.*MinKYCClient"
---

<objective>
Implement React hooks for the SDK and update the website with a dedicated Developer section.

Purpose: Lower the barrier for dApp developers to integrate MinKYC.
Output: useMinKYC hook in the SDK and a documentation section on the website.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sdk-compliance/04-01-SUMMARY.md
@website/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement React Hooks</name>
  <files>sdk/src/hooks/useMinKYC.ts, sdk/src/index.ts</files>
  <action>
    Add `react` and `react-dom` as peer dependencies to `sdk/package.json`.
    Implement `useMinKYC` hook in `sdk/src/hooks/useMinKYC.ts`:
    - Should manage loading/error state for verification.
    - Provide a `requestVerification` function that returns a QR code payload or deep link for the Mobile App.
    - Provide a `verifyProof` function that uses `MinKYCClient` to check the on-chain receipt.
    Export the hook from `sdk/src/index.ts`.
    Per D1.
  </action>
  <verify>
    <automated>npx tsc -p sdk/tsconfig.json</automated>
  </verify>
  <done>useMinKYC hook is implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Build Developer Section in Website</name>
  <files>website/src/components/DeveloperSection.tsx, website/src/App.tsx</files>
  <action>
    Create `website/src/components/DeveloperSection.tsx`.
    Include:
    - "Getting Started" guide (npm install @minkyc/sdk).
    - Code snippets for `useMinKYC`.
    - API Reference for `MinKYCClient`.
    - A live "Integration Playground" (mocking the mobile scan for demo purposes).
    Integrate the component into `website/src/App.tsx` (add a "Developers" link in the nav).
    Per W1.
  </action>
  <verify>
    <automated>cd website && npm run build</automated>
  </verify>
  <done>Website is updated with a functional and documented Developer section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Website → Local Storage | Storing session secrets for the playground |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | website/src/components/DeveloperSection.tsx | mitigate | Use clear "Demo/Mock" labels in the playground to prevent confusion with real verification |
</threat_model>

<verification>
- `npm run build` in both `sdk/` and `website/` succeeds.
- Manual check: Navigating to the Developer section shows the documentation.
</verification>

<success_criteria>
The SDK is easy to use via React hooks, and documentation is publicly available on the website.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sdk-compliance/04-02-SUMMARY.md`
</output>
