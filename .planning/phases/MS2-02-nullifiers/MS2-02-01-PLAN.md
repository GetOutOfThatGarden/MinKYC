---
phase: MS2-02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [circuits/src/main.nr]
autonomous: true
requirements: [P2.2.3]
must_haves:
  truths:
    - "Noir circuit uses Poseidon hash for nullifier generation"
    - "Circuit outputs a 32-byte nullifier compatible with Solana's Poseidon implementation"
  artifacts:
    - path: "circuits/src/main.nr"
      provides: "Poseidon-based nullifier logic"
  key_links:
    - from: "circuits/src/main.nr"
      to: "Solana Program"
      via: "Nullifier output match"
---

<objective>
Update the Noir circuit to use Poseidon hashing for nullifiers.

Purpose: Ensure the nullifier generated by the ZK circuit is compatible with the on-chain Poseidon-based address derivation used by Light Protocol.
Output: Refactored Noir circuit with Poseidon-based nullifier.
</objective>

<execution_context>
@/Users/user2/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/user2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@circuits/src/main.nr
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate Poseidon Hashing into Noir Circuit</name>
  <files>circuits/src/main.nr</files>
  <behavior>
    - The circuit must import `std::hash::poseidon`.
    - The nullifier must be computed as `poseidon([secret_nonce, verifier_id])`.
    - The circuit's return value must be the 32-byte nullifier hash (Field).
  </behavior>
  <action>
    1. Open `circuits/src/main.nr`.
    2. Replace `std::hash::pedersen_hash` with `std::hash::poseidon`.
    3. Update the `main` function to ensure the nullifier is correctly returned as a Field (which maps to [u8; 32] on-chain).
    4. Verify that the `over_18` logic remains intact.
  </action>
  <verify>
    <automated>cd circuits && nargo test</automated>
  </verify>
  <done>Circuit compiles and tests pass with Poseidon nullifier</done>
</task>

<task type="auto">
  <name>Task 2: Verify Nullifier Output Format</name>
  <files>circuits/src/main.nr</files>
  <action>
    1. Add a test case in `main.nr` that prints or asserts the output of the Poseidon hash for a known input.
    2. Ensure the output is a single Field, which is the standard format for Poseidon in Noir.
  </action>
  <verify>
    <automated>cd circuits && nargo test</automated>
  </verify>
  <done>Nullifier output is verified as a single Field</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Circuit -> Proof | Prover generates proof of Poseidon hash |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MS2-02-01 | Tampering | Nullifier Generation | mitigate | Use standard Noir library for Poseidon; verify against known test vectors if possible. |
</threat_model>

<verification>
Run `nargo test` in the `circuits` directory.
</verification>

<success_criteria>
- Circuit uses `std::hash::poseidon`.
- Nullifier is computed from `secret_nonce` and `verifier_id`.
- All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/MS2-02-nullifiers/MS2-02-01-SUMMARY.md`
</output>
